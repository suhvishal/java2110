CREATE TABLE BOOK (ID BIGINT AUTO_INCREMENT NOT NULL, DESCRIPTION VARCHAR(3000), ISBN VARCHAR(15), nb_of_pages INTEGER, publication_date DATE, TITLE VARCHAR(100), unit_cost FLOAT, PRIMARY KEY (ID))
CREATE TABLE CD (ID BIGINT AUTO_INCREMENT NOT NULL, DESCRIPTION VARCHAR(3000), GENRE VARCHAR(255), TITLE VARCHAR(100), total_duration FLOAT, unit_cost FLOAT, PRIMARY KEY (ID))
CREATE TABLE AUTHOR (ID BIGINT NOT NULL, BIO VARCHAR(5000), date_of_birth DATE, first_name VARCHAR(50), last_name VARCHAR(255) NOT NULL, PRIMARY KEY (ID))
CREATE TABLE BOOK_AUTHOR (Book_ID BIGINT NOT NULL, authors_ID BIGINT NOT NULL, PRIMARY KEY (Book_ID, authors_ID))
ALTER TABLE BOOK_AUTHOR ADD CONSTRAINT FK_BOOK_AUTHOR_Book_ID FOREIGN KEY (Book_ID) REFERENCES BOOK (ID)
ALTER TABLE BOOK_AUTHOR ADD CONSTRAINT FK_BOOK_AUTHOR_authors_ID FOREIGN KEY (authors_ID) REFERENCES AUTHOR (ID)
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT DECIMAL(38), PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('SEQ_GEN', 0)
